#include "main.h"

/**
 * int _abs- computes the absolute value of an integer.
 * followed by a new line
 * Return: Aways 0 (Sucess)
 */
int _abs(int num)
{
	if (num < 0)
	{
		return (num * -1);
	}
	return (num);
	return (0);
}
