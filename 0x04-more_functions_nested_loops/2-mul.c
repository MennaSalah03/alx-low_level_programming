#include "main.h"

/**
 * mul - multiplies twwo integers
 * by each other
 * @a: first operand
 * @b: second operand
 * Returns: always 0
 */

int mul(int a, int b)
{
	int product = a * b;
	
	return (product);
}
