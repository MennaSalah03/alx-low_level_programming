#include "main.h"

/**
 * _abs - prints the number with positive sign
 * @num: the int to be printed
 * Return: the nuber wit + sign
 */

int _abs(int num)
{
	if (num < 0)
	{
		return (num * -1);
	}
	return (num);
	return (0);
}
